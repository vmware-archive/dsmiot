#!/bin/bash
if [ $VCAP_APP_PORT]; 
   then
       PYTHON='.conda/bin/python'
       echo 'Using python:'$PYTHON
   else 
       PYTHON=`which python`
       echo 'Using python:'$PYTHON
fi
#Install predim
$PYTHON setup.py build
$PYTHON setup.py install
#Start the webserver
$PYTHON -m predim
